machine:
 ruby:
   version: ruby-2.3.4
 services:
   - docker
dependencies:
 pre:
   - gem install selenium-webdriver
 override:
   - gem install bundler
   - bundle install
test:
 override:
   - cucumber -p firefox -p dev -t @back
